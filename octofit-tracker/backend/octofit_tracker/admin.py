from .models import User, Team, Activity, Leaderboard, Workout

# Register models for admin panel (if needed for MongoDB, otherwise this is a placeholder)
# Admin registration is typically for Django ORM models, not MongoDB collections.